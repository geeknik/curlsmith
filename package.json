{
  "name": "curlsmith",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Firefox-first local API capture and styled cURL generator extension.",
  "scripts": {
    "dev:server": "node extension/test/integration/server.mjs",
    "test": "node --test extension/test/unit/*.test.mjs",
    "check:syntax": "find extension -name '*.js' -print0 | xargs -0 -n1 node --check && node --check extension/test/integration/server.mjs",
    "lint:addons": "addons-linter extension",
    "audit:runtime": "npm audit --omit=dev",
    "package:extension": "mkdir -p web-ext-artifacts && rm -f \"web-ext-artifacts/curlsmith-$npm_package_version.zip\" && cd extension && zip -qr \"../web-ext-artifacts/curlsmith-$npm_package_version.zip\" . -x 'test/*' 'docs/*' '.webextensionignore' '.web-extension-id' '.DS_Store' '*/.DS_Store'",
    "check": "npm test && npm run check:syntax && npm run lint:addons && npm run audit:runtime"
  },
  "devDependencies": {
    "addons-linter": "10.4.0"
  }
}
